// Kept in sync with scripts/seed.mjs's CATEGORIES (that script runs as
// plain Node outside the Next.js/TypeScript build, so it can't import this).
export const CATEGORIES = ["Bebidas", "Almacen", "Limpieza", "Perfumeria", "Snacks"] as const;
